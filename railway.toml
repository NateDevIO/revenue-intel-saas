[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"
watchPaths = ["backend/**"]

[deploy]
# Let Dockerfile CMD handle the startup - it properly handles $PORT variable
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
